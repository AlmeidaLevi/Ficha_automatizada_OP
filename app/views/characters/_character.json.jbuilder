json.extract! character, :id, :name, :origin, :character_class, :archetype, :current_pv, :current_pe, :current_sanity, :nex, :patent, :prestige_points, :element_affinity, :user_id, :for, :agi, :int, :pre, :vig, :created_at, :updated_at
json.url character_url(character, format: :json)
